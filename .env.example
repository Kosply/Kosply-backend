# LOCAL development (npm run dev / npm start)
NODE_ENV=development
PORT=3000
CORS_ORIGIN=*

# STAGING (npm run start:staging) — test here first, port 3001
# NODE_ENV=staging
# PORT=3001

# LIVE runs via PM2 env (ecosystem.config.js), do not use .env directly
# NODE_ENV=production
# PORT=3000
